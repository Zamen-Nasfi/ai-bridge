{
  "name": "ai-bridge",
  "version": "0.2.0",
  "private": true,
  "description": "A lightweight bridge between GPT and Claude.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
