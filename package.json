{
  "name": "ai-bridge",
  "version": "0.3.0",
  "private": true,
  "description": "A lightweight Cloudflare Worker bridge with a SQLite-backed Durable Object and controlled Cloudflare read-only gateway.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
